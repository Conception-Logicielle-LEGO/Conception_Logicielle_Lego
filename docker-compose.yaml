services:

  backend:
    build: ./backend
    env_file:
      - ./backend/.env
    network_mode: host

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
